"use client";

import { ArrowDown, ArrowUp, ChevronsLeft, ChevronsRight } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { TransactionFilters } from "@/components/transactions/transaction-filters";
import { currencyLabel, formatMoney, toPersianNumber } from "@/lib/formatters";
import type { CurrencyUnit, Filters, Transaction } from "@/lib/types";

type SortKey = "date" | "amount" | "description";
export function TransactionTable({ transactions, unit, filters, setFilters, accounts, categories, onSelect }: { transactions: Transaction[]; unit: CurrencyUnit; filters: Filters; setFilters: (f: Filters) => void; accounts: string[]; categories: { key: string; label: string }[]; onSelect: (t: Transaction) => void }) {
  const [page, setPage] = useState(1); const [pageSize, setPageSize] = useState(10); const [sort, setSort] = useState<SortKey>("date"); const [direction, setDirection] = useState<"asc"|"desc">("desc");
  useEffect(() => setPage(1), [transactions.length, filters]);
  const sorted = useMemo(() => [...transactions].sort((a,b) => { const av = sort === "date" ? a.date.getTime() : sort === "amount" ? Math.abs(a.amountIRR) : a.description; const bv = sort === "date" ? b.date.getTime() : sort === "amount" ? Math.abs(b.amountIRR) : b.description; const order = av < bv ? -1 : av > bv ? 1 : 0; return direction === "asc" ? order : -order; }), [transactions, sort, direction]);
  const pages = Math.max(1, Math.ceil(sorted.length / pageSize)); const visible = sorted.slice((page - 1) * pageSize, page * pageSize);
  const sortBy = (key: SortKey) => { if (sort === key) setDirection(direction === "asc" ? "desc" : "asc"); else { setSort(key); setDirection("desc"); } };
  return <section className="card overflow-hidden"><div className="p-4 md:p-5"><h2 className="font-bold">همه تراکنش‌ها</h2><p className="mt-1 text-xs text-[var(--muted)]">{toPersianNumber(transactions.length)} رکورد مطابق فیلترها</p></div><TransactionFilters {...{ filters, setFilters, accounts, categories }} /><div className="overflow-x-auto scrollbar-thin"><table className="w-full min-w-[860px] border-collapse text-right text-xs"><thead className="bg-[var(--surface-muted)] text-[var(--muted)]"><tr><th className="px-4 py-3 font-medium"><button onClick={() => sortBy("date")} className="flex items-center gap-1">تاریخ {sort === "date" && (direction === "asc" ? <ArrowUp size={12}/> : <ArrowDown size={12}/>)}</button></th><th className="px-4 py-3 font-medium"><button onClick={() => sortBy("description")} className="flex items-center gap-1">شرح {sort === "description" && (direction === "asc" ? <ArrowUp size={12}/> : <ArrowDown size={12}/>)}</button></th><th className="px-4 py-3 font-medium">حساب</th><th className="px-4 py-3 font-medium">دسته‌بندی</th><th className="px-4 py-3 font-medium">نوع</th><th className="px-4 py-3 text-left font-medium"><button onClick={() => sortBy("amount")} className="mr-auto flex items-center gap-1">مبلغ {sort === "amount" && (direction === "asc" ? <ArrowUp size={12}/> : <ArrowDown size={12}/>)}</button></th><th className="px-4 py-3 font-medium">واحد</th><th className="px-4 py-3 font-medium">وضعیت</th></tr></thead><tbody className="divide-y">{visible.map((t) => <tr key={t.id} onClick={() => onSelect(t)} className="cursor-pointer hover:bg-[var(--surface-muted)]"><td className="numbers whitespace-nowrap px-4 py-3.5">{toPersianNumber(t.jalaliDate)}</td><td className="max-w-[200px] truncate px-4 py-3.5 font-semibold">{t.description}</td><td className="px-4 py-3.5 text-[var(--muted)]">{t.account.raw}</td><td className="px-4 py-3.5">{t.account.categoryLabel}</td><td className="px-4 py-3.5"><Badge tone={t.account.type === "income" ? "positive" : t.account.type === "expenses" ? "negative" : "neutral"}>{t.account.typeLabel}</Badge></td><td className={`numbers whitespace-nowrap px-4 py-3.5 text-left font-bold ${t.account.type === "income" ? "text-[var(--accent)]" : t.account.type === "expenses" ? "text-[var(--danger)]" : ""}`}>{formatMoney(t.amountIRR, unit)}</td><td className="px-4 py-3.5">{currencyLabel(t.originalUnit)}</td><td className="px-4 py-3.5"><Badge tone={t.status.includes("انتظار") ? "warning" : "neutral"}>{t.status}</Badge></td></tr>)}</tbody></table>{!visible.length && <div className="p-12 text-center text-sm text-[var(--muted)]">تراکنشی مطابق فیلترها پیدا نشد.</div>}</div><div className="flex flex-wrap items-center justify-between gap-3 border-t p-4 text-xs text-[var(--muted)]"><div className="flex items-center gap-2">نمایش<select value={pageSize} onChange={(e) => { setPageSize(Number(e.target.value)); setPage(1); }} className="h-8 rounded-lg border bg-[var(--surface)] px-2"><option>10</option><option>20</option><option>50</option></select>ردیف</div><span>صفحه {toPersianNumber(page)} از {toPersianNumber(pages)}</span><div className="flex gap-1"><Button aria-label="صفحه قبل" size="icon" variant="outline" className="h-8 w-8" disabled={page <= 1} onClick={() => setPage(page - 1)}><ChevronsRight size={15}/></Button><Button aria-label="صفحه بعد" size="icon" variant="outline" className="h-8 w-8" disabled={page >= pages} onClick={() => setPage(page + 1)}><ChevronsLeft size={15}/></Button></div></div></section>;
}
