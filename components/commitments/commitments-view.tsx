"use client";

import { Check, ChevronDown, CircleDollarSign, Plus, UsersRound, WalletCards } from "lucide-react";
import { useMemo, useState } from "react";
import { Button } from "@/components/ui/button";
import { collectionStatus, commitmentStatus, friendsExpected, friendsReceived, friendsRemaining, friendStatus, providerRemaining } from "@/lib/financial-calculations";
import { formatMoney, toPersianNumber } from "@/lib/formatters";
import type { Commitment, CurrencyUnit, FriendReceivable, PaymentStatus } from "@/lib/types";

const statusLabel: Record<PaymentStatus, string> = { pending: "در انتظار", partially_paid: "بخشی پرداخت شده", paid: "تسویه شده" };
const statusClass: Record<PaymentStatus, string> = { pending: "bg-[var(--warning-soft)] text-[var(--warning)]", partially_paid: "bg-[var(--accent-soft)] text-[var(--accent-strong)]", paid: "bg-[#e8f7cf] text-[#456a17]" };
const money = (value: string) => Math.max(0, Math.round(Number(value.replaceAll(",", "")) || 0));

function Status({ value }: { value: PaymentStatus }) { return <span className={`inline-flex rounded-full px-2.5 py-1 text-[10px] font-bold ${statusClass[value]}`}>{statusLabel[value]}</span>; }

export function CommitmentsView({ commitments, unit, onChange }: { commitments: Commitment[]; unit: CurrencyUnit; onChange: (items: Commitment[]) => void }) {
  const [open, setOpen] = useState(false);
  const [name, setName] = useState(""); const [totalDue, setTotalDue] = useState(""); const [paidToProvider, setPaidToProvider] = useState(""); const [myShare, setMyShare] = useState(""); const [dueDate, setDueDate] = useState("");
  const [friends, setFriends] = useState<Array<{ name: string; expected: string }>>([{ name: "", expected: "" }]);
  const totalFriends = useMemo(() => friends.reduce((sum, friend) => sum + money(friend.expected), 0), [friends]);
  const canCreate = Boolean(name.trim() && money(totalDue) > 0 && money(myShare) + totalFriends === money(totalDue));
  const create = () => {
    if (!canCreate) return;
    const cleanFriends: FriendReceivable[] = friends.filter((friend) => friend.name.trim() && money(friend.expected) > 0).map((friend, index) => ({ id: `${Date.now()}-${index}`, name: friend.name.trim(), expected: money(friend.expected), received: 0 }));
    const next: Commitment = { id: `commitment-${Date.now()}`, name: name.trim(), totalDue: money(totalDue), paidToProvider: Math.min(money(paidToProvider), money(totalDue)), dueDate, myShare: money(myShare), receivables: cleanFriends };
    onChange([next, ...commitments]); setName(""); setTotalDue(""); setPaidToProvider(""); setMyShare(""); setDueDate(""); setFriends([{ name: "", expected: "" }]); setOpen(false);
  };
  const receive = (commitmentId: string, friendId: string, amount: number) => onChange(commitments.map((commitment) => commitment.id !== commitmentId ? commitment : { ...commitment, receivables: commitment.receivables.map((friend) => friend.id !== friendId ? friend : { ...friend, received: Math.min(friend.expected, friend.received + amount) }) }));

  return <div className="space-y-5">
    <section className="card flex flex-col gap-5 p-6 md:flex-row md:items-center md:justify-between"><div className="flex items-center gap-4"><span className="soft-accent flex h-12 w-12 items-center justify-center rounded-2xl"><WalletCards size={22}/></span><div><h2 className="text-xl font-black tracking-tight">تعهدهای گروهی</h2><p className="mt-1 text-xs text-[var(--muted)]">پرداخت سرویس و وصول سهم دوستان به‌صورت مستقل ثبت و دنبال می‌شوند.</p><p className="mt-1 text-[10px] text-[var(--muted)]">پرداخت‌ها و وصول‌هایی که اینجا ثبت می‌کنید را دوباره به‌عنوان تراکنش نقدی وارد نکنید؛ از دوباره‌شماری جلوگیری می‌شود.</p></div></div><Button onClick={() => setOpen(!open)}><Plus size={16}/>{open ? "بستن فرم" : "افزودن تعهد"}</Button></section>
    {open && <section className="card p-5 md:p-6"><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><Field label="نام تعهد" value={name} onChange={setName} placeholder="مثلاً Digipay - ویلا"/><Field label="مبلغ کل تعهد" value={totalDue} onChange={setTotalDue} type="number"/><Field label="پرداخت به سرویس" value={paidToProvider} onChange={setPaidToProvider} type="number"/><Field label="سهم من" value={myShare} onChange={setMyShare} type="number"/><Field label="سررسید" value={dueDate} onChange={setDueDate} type="date"/></div><div className="mt-6 border-t pt-5"><div className="flex items-center justify-between"><div><h3 className="text-sm font-bold">سهم دوستان</h3><p className="mt-1 text-[11px] text-[var(--muted)]">جمع ثبت‌شده: {formatMoney(totalFriends * 10, unit)} · سهم‌ها باید دقیقاً با کل تعهد برابر شوند.</p></div><button onClick={() => setFriends([...friends, { name: "", expected: "" }])} className="text-xs font-bold text-[var(--accent-strong)]">افزودن دوست</button></div><div className="mt-3 space-y-2">{friends.map((friend, index) => <div key={index} className="grid gap-2 sm:grid-cols-[1fr_180px]"><input className="h-10 rounded-xl border bg-[var(--surface)] px-3 text-xs outline-none focus:border-[var(--accent)]" value={friend.name} onChange={(event) => setFriends(friends.map((item, i) => i === index ? { ...item, name: event.target.value } : item))} placeholder="نام دوست"/><input className="h-10 rounded-xl border bg-[var(--surface)] px-3 text-xs outline-none focus:border-[var(--accent)]" type="number" value={friend.expected} onChange={(event) => setFriends(friends.map((item, i) => i === index ? { ...item, expected: event.target.value } : item))} placeholder="مبلغ سهم به تومان"/></div>)}</div></div><div className="mt-6 flex justify-end"><Button disabled={!canCreate} onClick={create}>ثبت تعهد</Button></div></section>}
    {!commitments.length ? <section className="card p-10 text-center"><UsersRound className="mx-auto text-[var(--accent)]" size={30}/><h3 className="mt-4 font-bold">تعهد گروهی ثبت نشده است</h3><p className="mt-2 text-xs text-[var(--muted)]">برای شروع، پرداخت یک سرویس و سهم هر دوست را ثبت کنید.</p></section> : commitments.map((commitment) => <CommitmentCard key={commitment.id} commitment={commitment} unit={unit} onReceive={receive}/>) }
  </div>;
}

function Field({ label, value, onChange, type = "text", placeholder }: { label: string; value: string; onChange: (value: string) => void; type?: string; placeholder?: string }) { return <label className="block text-xs font-semibold text-[var(--muted)]">{label}<input className="mt-2 h-11 w-full rounded-xl border bg-[var(--surface)] px-3 text-sm font-normal text-[var(--foreground)] outline-none focus:border-[var(--accent)]" type={type} value={value} onChange={(event) => onChange(event.target.value)} placeholder={placeholder}/></label>; }

function CommitmentCard({ commitment, unit, onReceive }: { commitment: Commitment; unit: CurrencyUnit; onReceive: (commitmentId: string, friendId: string, amount: number) => void }) {
  const [expanded, setExpanded] = useState(false); const [amounts, setAmounts] = useState<Record<string, string>>({}); const provider = commitmentStatus(commitment); const collection = collectionStatus(commitment);
  return <section className="card overflow-hidden"><div className="flex flex-col gap-5 p-5 md:p-6 xl:flex-row xl:items-start xl:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-lg font-black">{commitment.name}</h2><Status value={provider}/><span className="text-[10px] text-[var(--muted)]">وصول دوستان: <Status value={collection}/></span></div><p className="mt-2 text-xs text-[var(--muted)]">سررسید: {commitment.dueDate || "ثبت نشده"}</p></div><button className="inline-flex items-center gap-1 text-xs font-bold text-[var(--accent-strong)]" onClick={() => setExpanded(!expanded)}>{expanded ? "بستن جزئیات" : "دیدن جزئیات"}<ChevronDown className={expanded ? "rotate-180" : ""} size={15}/></button></div><div className="grid grid-cols-2 border-y bg-[var(--surface-muted)] md:grid-cols-4 xl:grid-cols-8">{[["کل تعهد", commitment.totalDue], ["پرداخت به سرویس", commitment.paidToProvider], ["باقی‌مانده سرویس", providerRemaining(commitment)], ["سهم من", commitment.myShare], ["سهم دوستان", friendsExpected(commitment)], ["دریافت از دوستان", friendsReceived(commitment)], ["طلب باقی‌مانده", friendsRemaining(commitment)]].map(([label, value]) => <div key={String(label)} className="border-b p-4 last:border-b-0 md:border-b-0 xl:border-l xl:last:border-l-0"><p className="text-[10px] text-[var(--muted)]">{label}</p><p className="numbers mt-2 truncate text-sm font-extrabold">{formatMoney(Number(value) * 10, unit)}</p></div>)}</div>{expanded && <div className="p-5 md:p-6"><div className="mb-3 flex items-center gap-2"><UsersRound size={17} className="text-[var(--accent)]"/><h3 className="font-bold">وضعیت دوستان</h3></div><div className="overflow-x-auto"><div className="min-w-[620px]"><div className="grid grid-cols-[1fr_130px_130px_130px_110px_170px] gap-3 border-b pb-3 text-[10px] text-[var(--muted)]"><span>نام</span><span>سهم</span><span>دریافت‌شده</span><span>باقی‌مانده</span><span>وضعیت</span><span>ثبت پرداخت</span></div>{commitment.receivables.map((friend) => { const remaining = Math.max(0, friend.expected - friend.received); const status = friendStatus(friend); return <div key={friend.id} className="grid grid-cols-[1fr_130px_130px_130px_110px_170px] items-center gap-3 border-b py-3 text-xs"><strong>{friend.name}</strong><span className="numbers">{formatMoney(friend.expected * 10, unit)}</span><span className="numbers">{formatMoney(friend.received * 10, unit)}</span><span className="numbers">{formatMoney(remaining * 10, unit)}</span><Status value={status}/><div className="flex gap-1"><input aria-label={`مبلغ پرداخت ${friend.name}`} className="h-8 w-24 rounded-lg border bg-[var(--surface)] px-2 text-[11px]" type="number" disabled={!remaining} value={amounts[friend.id] ?? ""} onChange={(event) => setAmounts({ ...amounts, [friend.id]: event.target.value })} placeholder="تومان"/><button disabled={!remaining} onClick={() => { const value = Math.min(remaining, money(amounts[friend.id] || String(remaining))); if (value) { onReceive(commitment.id, friend.id, value); setAmounts({ ...amounts, [friend.id]: "" }); } }} className="inline-flex h-8 items-center gap-1 rounded-lg bg-[var(--accent)] px-2 text-[10px] font-bold text-white disabled:opacity-50"><Check size={13}/>ثبت</button></div></div>; })}</div></div></div>}</section>;
}
